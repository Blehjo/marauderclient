import { Component } from "react";
import { NoteContainer } from "../../styles/note/note.styles";

class Note extends Component {
    render() {
        return(
            <NoteContainer>
                This is the note container!  
            </NoteContainer>
        );
    }
}

export default Note;